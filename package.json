{
  "name": "customssd-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node playbeat-backend/server.js",
    "postinstall": "cd playbeat-backend && npm install --silent"
  },
  "engines": {
    "node": ">=18"
  }
}
